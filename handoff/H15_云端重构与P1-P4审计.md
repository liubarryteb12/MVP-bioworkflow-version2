# Handoff H15 — 云端仓库重构 + P1-P4 文档审计

- handoff_id: `H15_A2USER_云端重构与P1-P4审计`
- from: A（agent）
- to: USER / 下一 agent
- time: 2026-09-17T18:00:00+08:00
- 前驱: `H14_A2USER_S21缺口补齐`

---

## 1 任务来源（USER 指令）

> "把云端的改成现在的（=第一版）；推 2 个分支分别装第一版和第二版内容；把第一版能跑通的、第2版能跑通的推到主目录；待 P1-P2-P3-P4 全流程跑通后，审查合并分支中可行内容（顺 P1→P2→P3→P4 来）。"

## 2 执行结果

### 2.1 云端仓库重构（已推送，origin = `liubarryteb12/MVP-bioworkflow-version2`）

| 分支 | 内容 | 说明 |
|---|---|---|
| **`main`** | 第一版可运行框架（`ddf3a39`） | 根目录 `crossval/ handoff/ analysis/ build.py` + `.github/workflows/crossval_verify.yml`，116 文件。即"云端的 = 第一版" |
| **`第二版`** | v2 框架快照（`cf1265e`） | 原云端内容整体保留，待后续审查合并 |
| **`第一版`** | 第一版完整暂存 | `crossval_verify.yml` + 内层 `_repo/`（v1 全量框架） |

- 本地 clone：`第二版/04.云端验证_GitHubActions/1_发到云端的/_repo`

### 2.2 数据安全（上机前最关键）

- **排除所有 `*.gz`**：第二版 `4-P1生信分析` 371 个 GEO 原始数据未入库。
- **排除嵌套 `.git`**：两版本的 `1_发到云端的/_repo/` 各自是独立 git 仓库，用 `robocopy /XD .git` + 二次清理，无嵌套仓库污染。
- 上机触发：`gh workflow run crossval_verify.yml`（需批准）；push main **不会**自动全量跑（需 flag 文件或 dispatch）。

## 3 P1-P4 文档审计结论

### 3.1 已修（16 处）

CL-1 改名残留 12 处；P4§6.1 孤儿表头；P4§9.1 rank2/3 总分；P4§13.1 裸号 P1/P4 → `PRED.P1`/`PRED.P4`；P3§28.3.5 字号；P3/P4 目录树。

### 3.2 命名策略待决（最大类，须用户拍板）

规则①(§567)要求四部分全前缀 + 全局唯一，但执行远未完成且规则自相矛盾：

- §562 自相矛盾：「P3-T1…T69」 vs 「P3 内记 T*/F*/D*」。
- P3 主判据 `T1–T69 / F1–F29 / D1–D14` 全裸号；实锤 `T1` 在 P1(3789)/P2(4157)/P3(4631) 三处同名，`D1` 在 3543/4154/5056 三处 → 违反全局唯一。
- P4 主判据裸号 `A1–A7/B1–B7/C1–C9/E1–E4/S1–S5/M1–M4/J1–J5/O1–O4/I1–I7`，但规则 §563/§565 要求 `P4-A1…` 带前缀 → 违反自身规则。
- P2 非 WP 判据全裸号 `G1–G12/W1–W8/R1–R9/N1–N3/M1–M3/T1–T5/H1–H3/I1–I4/NC-1–7/S1–S5/D1–D3`；`M1/S1/I1` 还与 P4 裸号重名。
- P1 §14.3(3789) 裸 `T1–T5` 与 §426「P1-T1–T14 系凭空已撤销」自相矛盾。

### 3.3 遗留待决（未强改，8 项）

a) §22.1 A 档 66 vs §2.4 PATH-90/91 计入档位（66/68 冲突）；
b) P3-FIGF-31 线条图 ≥600 vs F7/§13.1/§28.3.4 线条图 1200 冲突；
c) P4§11.3 示例 yaml ≠ items 数组 schema，「P4-P5」疑应 `PRED.P1`；
d) P2§21.1 消融表用例号旧式；
e) §9.4/§9.5 悬空引用；
f) §18.1「≥8pt」与底线 7pt 张力；
g) 计数不符（§14「117」vs §17「114」；§17 合计343/去重339 vs workspace.yaml 339）；
h) `writing_guard.py`（第一版）仍用逻辑链 P0–P4，复用需改名。

## 4 关键架构真相（影响"上机能校验什么"）

- 云端 `end_to_end_validation.yml` 跑**框架执行**（R/Python 跑真实 GEO 数据），不读 markdown、不校验判据 ID。
- spec 规定的 `criteria_registry.yaml` 在仓库**缺失**；那份 spec markdown 不在仓库内（workspace.yaml:96 指向它，但仓库只有框架）。
- `workspace.yaml:97` 写死 339、`:103` 残留 `G-T90/G-T91`。
- → 要让上机校验 spec，需：(a) 生成 `criteria_registry.yaml` 入仓；(b) workflow 增 lint 步。

## 5 待办 / 阻塞

1. **用户定命名总策略**（全局唯一 vs 裸号）→ 决定 §3.2 如何收口。
2. （可选）生成 `criteria_registry.yaml` 入仓 + workflow 增 lint 步 → 上机机检 spec。
3. P1-P4 全流程跑通后，按 `P1→P2→P3→P4` 顺序审查两分支可行内容合并入 main。

## 6 状态

- `state_out`：已执行（云端重构+推送完成；审计完成；数据安全已确认）
- `ack`：`null`（在途，待用户确认）
- `next`：`null`（链尾：待用户定策略 + P1-P4 跑通后合并）
